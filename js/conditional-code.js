

// let characterCount = document.querySelector('#product-name').value.length;
// let characterCount = parseInt(document.querySelector('.character-count').textContent);

if (document.querySelector('#product-name').value.length === 50) {
    console.log(document.querySelector('#product-name').value.length)
    // document.querySelector('.character-count').classList.add('#remaining-chars.warning');
}